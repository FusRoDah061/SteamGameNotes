<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:SteamGameNotes">

    <SolidColorBrush x:Key="ColorBackground" Color="#29303A"/>
    <SolidColorBrush x:Key="ColorTypo" Color="#808080"/>
    <SolidColorBrush x:Key="ColorText" Color="#C5CBD8"/>
    <SolidColorBrush x:Key="ColorBackgroundDarker" Color="#1E2024"/>
    <SolidColorBrush x:Key="ColorBackgroundDarkerHover" Color="#17181B"/>

    <Style TargetType="TextBox">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Cursor" Value="IBeam"/>
        <Setter Property="AllowDrop" Value="true"/>
        <Setter Property="BorderBrush" Value="{x:Null}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Foreground" Value="{StaticResource ColorText}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="TextAlignment" Value="Left"/>
        <Setter Property="CaretBrush" Value="{StaticResource ColorText}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Grid>
                        <Border x:Name="background" Background="{StaticResource ColorBackgroundDarker}" CornerRadius="4"/>
                        <ContentPresenter HorizontalAlignment="Left"
                            VerticalAlignment="Center" />
                        <ScrollViewer Margin="0" x:Name="PART_ContentHost"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="true">
                            <Setter TargetName="background" Property="Background" Value="{StaticResource ColorBackgroundDarkerHover}" />
                        </Trigger>

                        <DataTrigger Binding="{Binding Path=Text}" Value="">
                            <Setter Property="Foreground" Value="LightGray"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Button">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="background" Background="{StaticResource ColorBackgroundDarker}" CornerRadius="4"/>
                        <ContentPresenter HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="background" Property="Background" Value="{StaticResource ColorBackgroundDarkerHover}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>